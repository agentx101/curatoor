import {
  require_buffer
} from "./chunk-PIX3PJKD.js";
import {
  __toESM
} from "./chunk-JZQ37OGZ.js";

// node_modules/proxy-compare/dist/index.modern.js
var e = Symbol();
var t = Symbol();
var s = Object.getPrototypeOf;
var c = /* @__PURE__ */ new WeakMap();
var l = (e2) => e2 && (c.has(e2) ? c.get(e2) : s(e2) === Object.prototype || s(e2) === Array.prototype);
var y = (e2) => l(e2) && e2[t] || null;
var h = (e2, t2 = true) => {
  c.set(e2, t2);
};

// node_modules/valtio/esm/vanilla.mjs
var isObject = (x2) => typeof x2 === "object" && x2 !== null;
var proxyStateMap = /* @__PURE__ */ new WeakMap();
var refSet = /* @__PURE__ */ new WeakSet();
var buildProxyFunction = (objectIs = Object.is, newProxy = (target, handler) => new Proxy(target, handler), canProxy = (x2) => isObject(x2) && !refSet.has(x2) && (Array.isArray(x2) || !(Symbol.iterator in x2)) && !(x2 instanceof WeakMap) && !(x2 instanceof WeakSet) && !(x2 instanceof Error) && !(x2 instanceof Number) && !(x2 instanceof Date) && !(x2 instanceof String) && !(x2 instanceof RegExp) && !(x2 instanceof ArrayBuffer), defaultHandlePromise = (promise) => {
  switch (promise.status) {
    case "fulfilled":
      return promise.value;
    case "rejected":
      throw promise.reason;
    default:
      throw promise;
  }
}, snapCache = /* @__PURE__ */ new WeakMap(), createSnapshot = (target, version, handlePromise = defaultHandlePromise) => {
  const cache = snapCache.get(target);
  if ((cache == null ? void 0 : cache[0]) === version) {
    return cache[1];
  }
  const snap = Array.isArray(target) ? [] : Object.create(Object.getPrototypeOf(target));
  h(snap, true);
  snapCache.set(target, [version, snap]);
  Reflect.ownKeys(target).forEach((key) => {
    if (Object.getOwnPropertyDescriptor(snap, key)) {
      return;
    }
    const value = Reflect.get(target, key);
    const desc = {
      value,
      enumerable: true,
      // This is intentional to avoid copying with proxy-compare.
      // It's still non-writable, so it avoids assigning a value.
      configurable: true
    };
    if (refSet.has(value)) {
      h(value, false);
    } else if (value instanceof Promise) {
      delete desc.value;
      desc.get = () => handlePromise(value);
    } else if (proxyStateMap.has(value)) {
      const [target2, ensureVersion] = proxyStateMap.get(
        value
      );
      desc.value = createSnapshot(
        target2,
        ensureVersion(),
        handlePromise
      );
    }
    Object.defineProperty(snap, key, desc);
  });
  return snap;
}, proxyCache = /* @__PURE__ */ new WeakMap(), versionHolder = [1, 1], proxyFunction = (initialObject) => {
  if (!isObject(initialObject)) {
    throw new Error("object required");
  }
  const found = proxyCache.get(initialObject);
  if (found) {
    return found;
  }
  let version = versionHolder[0];
  const listeners = /* @__PURE__ */ new Set();
  const notifyUpdate = (op, nextVersion = ++versionHolder[0]) => {
    if (version !== nextVersion) {
      version = nextVersion;
      listeners.forEach((listener) => listener(op, nextVersion));
    }
  };
  let checkVersion = versionHolder[1];
  const ensureVersion = (nextCheckVersion = ++versionHolder[1]) => {
    if (checkVersion !== nextCheckVersion && !listeners.size) {
      checkVersion = nextCheckVersion;
      propProxyStates.forEach(([propProxyState]) => {
        const propVersion = propProxyState[1](nextCheckVersion);
        if (propVersion > version) {
          version = propVersion;
        }
      });
    }
    return version;
  };
  const createPropListener = (prop) => (op, nextVersion) => {
    const newOp = [...op];
    newOp[1] = [prop, ...newOp[1]];
    notifyUpdate(newOp, nextVersion);
  };
  const propProxyStates = /* @__PURE__ */ new Map();
  const addPropListener = (prop, propProxyState) => {
    if ((import.meta.env ? import.meta.env.MODE : void 0) !== "production" && propProxyStates.has(prop)) {
      throw new Error("prop listener already exists");
    }
    if (listeners.size) {
      const remove = propProxyState[3](createPropListener(prop));
      propProxyStates.set(prop, [propProxyState, remove]);
    } else {
      propProxyStates.set(prop, [propProxyState]);
    }
  };
  const removePropListener = (prop) => {
    var _a;
    const entry = propProxyStates.get(prop);
    if (entry) {
      propProxyStates.delete(prop);
      (_a = entry[1]) == null ? void 0 : _a.call(entry);
    }
  };
  const addListener = (listener) => {
    listeners.add(listener);
    if (listeners.size === 1) {
      propProxyStates.forEach(([propProxyState, prevRemove], prop) => {
        if ((import.meta.env ? import.meta.env.MODE : void 0) !== "production" && prevRemove) {
          throw new Error("remove already exists");
        }
        const remove = propProxyState[3](createPropListener(prop));
        propProxyStates.set(prop, [propProxyState, remove]);
      });
    }
    const removeListener = () => {
      listeners.delete(listener);
      if (listeners.size === 0) {
        propProxyStates.forEach(([propProxyState, remove], prop) => {
          if (remove) {
            remove();
            propProxyStates.set(prop, [propProxyState]);
          }
        });
      }
    };
    return removeListener;
  };
  const baseObject = Array.isArray(initialObject) ? [] : Object.create(Object.getPrototypeOf(initialObject));
  const handler = {
    deleteProperty(target, prop) {
      const prevValue = Reflect.get(target, prop);
      removePropListener(prop);
      const deleted = Reflect.deleteProperty(target, prop);
      if (deleted) {
        notifyUpdate(["delete", [prop], prevValue]);
      }
      return deleted;
    },
    set(target, prop, value, receiver) {
      const hasPrevValue = Reflect.has(target, prop);
      const prevValue = Reflect.get(target, prop, receiver);
      if (hasPrevValue && (objectIs(prevValue, value) || proxyCache.has(value) && objectIs(prevValue, proxyCache.get(value)))) {
        return true;
      }
      removePropListener(prop);
      if (isObject(value)) {
        value = y(value) || value;
      }
      let nextValue = value;
      if (value instanceof Promise) {
        value.then((v) => {
          value.status = "fulfilled";
          value.value = v;
          notifyUpdate(["resolve", [prop], v]);
        }).catch((e2) => {
          value.status = "rejected";
          value.reason = e2;
          notifyUpdate(["reject", [prop], e2]);
        });
      } else {
        if (!proxyStateMap.has(value) && canProxy(value)) {
          nextValue = proxyFunction(value);
        }
        const childProxyState = !refSet.has(nextValue) && proxyStateMap.get(nextValue);
        if (childProxyState) {
          addPropListener(prop, childProxyState);
        }
      }
      Reflect.set(target, prop, nextValue, receiver);
      notifyUpdate(["set", [prop], value, prevValue]);
      return true;
    }
  };
  const proxyObject = newProxy(baseObject, handler);
  proxyCache.set(initialObject, proxyObject);
  const proxyState = [
    baseObject,
    ensureVersion,
    createSnapshot,
    addListener
  ];
  proxyStateMap.set(proxyObject, proxyState);
  Reflect.ownKeys(initialObject).forEach((key) => {
    const desc = Object.getOwnPropertyDescriptor(
      initialObject,
      key
    );
    if ("value" in desc) {
      proxyObject[key] = initialObject[key];
      delete desc.value;
      delete desc.writable;
    }
    Object.defineProperty(baseObject, key, desc);
  });
  return proxyObject;
}) => [
  // public functions
  proxyFunction,
  // shared state
  proxyStateMap,
  refSet,
  // internal things
  objectIs,
  newProxy,
  canProxy,
  defaultHandlePromise,
  snapCache,
  createSnapshot,
  proxyCache,
  versionHolder
];
var [defaultProxyFunction] = buildProxyFunction();
function proxy(initialObject = {}) {
  return defaultProxyFunction(initialObject);
}
function subscribe(proxyObject, callback, notifyInSync) {
  const proxyState = proxyStateMap.get(proxyObject);
  if ((import.meta.env ? import.meta.env.MODE : void 0) !== "production" && !proxyState) {
    console.warn("Please use proxy object");
  }
  let promise;
  const ops = [];
  const addListener = proxyState[3];
  let isListenerActive = false;
  const listener = (op) => {
    ops.push(op);
    if (notifyInSync) {
      callback(ops.splice(0));
      return;
    }
    if (!promise) {
      promise = Promise.resolve().then(() => {
        promise = void 0;
        if (isListenerActive) {
          callback(ops.splice(0));
        }
      });
    }
  };
  const removeListener = addListener(listener);
  isListenerActive = true;
  return () => {
    isListenerActive = false;
    removeListener();
  };
}
function snapshot(proxyObject, handlePromise) {
  const proxyState = proxyStateMap.get(proxyObject);
  if ((import.meta.env ? import.meta.env.MODE : void 0) !== "production" && !proxyState) {
    console.warn("Please use proxy object");
  }
  const [target, ensureVersion, createSnapshot] = proxyState;
  return createSnapshot(target, ensureVersion(), handlePromise);
}

// node_modules/@walletconnect/modal-core/dist/index.js
var import_buffer = __toESM(require_buffer());
var n = proxy({ history: ["ConnectWallet"], view: "ConnectWallet", data: void 0 });
var N = { state: n, subscribe(e2) {
  return subscribe(n, () => e2(n));
}, push(e2, t2) {
  e2 !== n.view && (n.view = e2, t2 && (n.data = t2), n.history.push(e2));
}, reset(e2) {
  n.view = e2, n.history = [e2];
}, replace(e2) {
  n.history.length > 1 && (n.history[n.history.length - 1] = e2, n.view = e2);
}, goBack() {
  if (n.history.length > 1) {
    n.history.pop();
    const [e2] = n.history.slice(-1);
    n.view = e2;
  }
}, setData(e2) {
  n.data = e2;
} };
var i = { WALLETCONNECT_DEEPLINK_CHOICE: "WALLETCONNECT_DEEPLINK_CHOICE", WCM_VERSION: "WCM_VERSION", RECOMMENDED_WALLET_AMOUNT: 9, isMobile() {
  return typeof window < "u" ? Boolean(window.matchMedia("(pointer:coarse)").matches || /Android|webOS|iPhone|iPad|iPod|BlackBerry|Opera Mini/u.test(navigator.userAgent)) : false;
}, isAndroid() {
  return i.isMobile() && navigator.userAgent.toLowerCase().includes("android");
}, isIos() {
  const e2 = navigator.userAgent.toLowerCase();
  return i.isMobile() && (e2.includes("iphone") || e2.includes("ipad"));
}, isHttpUrl(e2) {
  return e2.startsWith("http://") || e2.startsWith("https://");
}, isArray(e2) {
  return Array.isArray(e2) && e2.length > 0;
}, formatNativeUrl(e2, t2, s2) {
  if (i.isHttpUrl(e2))
    return this.formatUniversalUrl(e2, t2, s2);
  let o = e2;
  o.includes("://") || (o = e2.replaceAll("/", "").replaceAll(":", ""), o = `${o}://`), o.endsWith("/") || (o = `${o}/`), this.setWalletConnectDeepLink(o, s2);
  const a = encodeURIComponent(t2);
  return `${o}wc?uri=${a}`;
}, formatUniversalUrl(e2, t2, s2) {
  if (!i.isHttpUrl(e2))
    return this.formatNativeUrl(e2, t2, s2);
  let o = e2;
  o.endsWith("/") || (o = `${o}/`), this.setWalletConnectDeepLink(o, s2);
  const a = encodeURIComponent(t2);
  return `${o}wc?uri=${a}`;
}, async wait(e2) {
  return new Promise((t2) => {
    setTimeout(t2, e2);
  });
}, openHref(e2, t2) {
  window.open(e2, t2, "noreferrer noopener");
}, setWalletConnectDeepLink(e2, t2) {
  try {
    localStorage.setItem(i.WALLETCONNECT_DEEPLINK_CHOICE, JSON.stringify({ href: e2, name: t2 }));
  } catch {
    console.info("Unable to set WalletConnect deep link");
  }
}, setWalletConnectAndroidDeepLink(e2) {
  try {
    const [t2] = e2.split("?");
    localStorage.setItem(i.WALLETCONNECT_DEEPLINK_CHOICE, JSON.stringify({ href: t2, name: "Android" }));
  } catch {
    console.info("Unable to set WalletConnect android deep link");
  }
}, removeWalletConnectDeepLink() {
  try {
    localStorage.removeItem(i.WALLETCONNECT_DEEPLINK_CHOICE);
  } catch {
    console.info("Unable to remove WalletConnect deep link");
  }
}, setModalVersionInStorage() {
  try {
    typeof localStorage < "u" && localStorage.setItem(i.WCM_VERSION, "2.5.9");
  } catch {
    console.info("Unable to set Web3Modal version in storage");
  }
}, getWalletRouterData() {
  var e2;
  const t2 = (e2 = N.state.data) == null ? void 0 : e2.Wallet;
  if (!t2)
    throw new Error('Missing "Wallet" view data');
  return t2;
} };
var T = typeof location < "u" && (location.hostname.includes("localhost") || location.protocol.includes("https"));
var r = proxy({ enabled: T, userSessionId: "", events: [], connectedWalletId: void 0 });
var $ = { state: r, subscribe(e2) {
  return subscribe(r.events, () => e2(snapshot(r.events[r.events.length - 1])));
}, initialize() {
  r.enabled && typeof (crypto == null ? void 0 : crypto.randomUUID) < "u" && (r.userSessionId = crypto.randomUUID());
}, setConnectedWalletId(e2) {
  r.connectedWalletId = e2;
}, click(e2) {
  if (r.enabled) {
    const t2 = { type: "CLICK", name: e2.name, userSessionId: r.userSessionId, timestamp: Date.now(), data: e2 };
    r.events.push(t2);
  }
}, track(e2) {
  if (r.enabled) {
    const t2 = { type: "TRACK", name: e2.name, userSessionId: r.userSessionId, timestamp: Date.now(), data: e2 };
    r.events.push(t2);
  }
}, view(e2) {
  if (r.enabled) {
    const t2 = { type: "VIEW", name: e2.name, userSessionId: r.userSessionId, timestamp: Date.now(), data: e2 };
    r.events.push(t2);
  }
} };
var l2 = proxy({ chains: void 0, walletConnectUri: void 0, isAuth: false, isCustomDesktop: false, isCustomMobile: false, isDataLoaded: false, isUiLoaded: false });
var c2 = { state: l2, subscribe(e2) {
  return subscribe(l2, () => e2(l2));
}, setChains(e2) {
  l2.chains = e2;
}, setWalletConnectUri(e2) {
  l2.walletConnectUri = e2;
}, setIsCustomDesktop(e2) {
  l2.isCustomDesktop = e2;
}, setIsCustomMobile(e2) {
  l2.isCustomMobile = e2;
}, setIsDataLoaded(e2) {
  l2.isDataLoaded = e2;
}, setIsUiLoaded(e2) {
  l2.isUiLoaded = e2;
}, setIsAuth(e2) {
  l2.isAuth = e2;
} };
var y2 = proxy({ projectId: "", mobileWallets: void 0, desktopWallets: void 0, walletImages: void 0, chains: void 0, enableAuthMode: false, enableExplorer: true, explorerExcludedWalletIds: void 0, explorerRecommendedWalletIds: void 0, termsOfServiceUrl: void 0, privacyPolicyUrl: void 0 });
var C = { state: y2, subscribe(e2) {
  return subscribe(y2, () => e2(y2));
}, setConfig(e2) {
  var t2, s2;
  $.initialize(), c2.setChains(e2.chains), c2.setIsAuth(Boolean(e2.enableAuthMode)), c2.setIsCustomMobile(Boolean((t2 = e2.mobileWallets) == null ? void 0 : t2.length)), c2.setIsCustomDesktop(Boolean((s2 = e2.desktopWallets) == null ? void 0 : s2.length)), i.setModalVersionInStorage(), Object.assign(y2, e2);
} };
var L = "https://explorer-api.walletconnect.com";
async function W(e2, t2) {
  const s2 = new URL(e2, L);
  return s2.searchParams.append("projectId", C.state.projectId), Object.entries(t2).forEach(([o, a]) => {
    a && s2.searchParams.append(o, String(a));
  }), (await fetch(s2)).json();
}
var m = { async getDesktopListings(e2) {
  return W("/w3m/v1/getDesktopListings", e2);
}, async getMobileListings(e2) {
  return W("/w3m/v1/getMobileListings", e2);
}, async getInjectedListings(e2) {
  return W("/w3m/v1/getInjectedListings", e2);
}, async getAllListings(e2) {
  return W("/w3m/v1/getAllListings", e2);
}, getWalletImageUrl(e2) {
  return `${L}/w3m/v1/getWalletImage/${e2}?projectId=${C.state.projectId}`;
}, getAssetImageUrl(e2) {
  return `${L}/w3m/v1/getAssetImage/${e2}?projectId=${C.state.projectId}`;
} };
var P = Object.defineProperty;
var O = Object.getOwnPropertySymbols;
var _ = Object.prototype.hasOwnProperty;
var R = Object.prototype.propertyIsEnumerable;
var U = (e2, t2, s2) => t2 in e2 ? P(e2, t2, { enumerable: true, configurable: true, writable: true, value: s2 }) : e2[t2] = s2;
var B = (e2, t2) => {
  for (var s2 in t2 || (t2 = {}))
    _.call(t2, s2) && U(e2, s2, t2[s2]);
  if (O)
    for (var s2 of O(t2))
      R.call(t2, s2) && U(e2, s2, t2[s2]);
  return e2;
};
var M = i.isMobile();
var d = proxy({ wallets: { listings: [], total: 0, page: 1 }, search: { listings: [], total: 0, page: 1 }, recomendedWallets: [] });
var G = { state: d, async getRecomendedWallets() {
  const { explorerRecommendedWalletIds: e2, explorerExcludedWalletIds: t2 } = C.state;
  if (e2 === "NONE" || t2 === "ALL" && !e2)
    return d.recomendedWallets;
  if (i.isArray(e2)) {
    const s2 = { recommendedIds: e2.join(",") }, { listings: o } = await m.getAllListings(s2), a = Object.values(o);
    a.sort((u, v) => {
      const w = e2.indexOf(u.id), f = e2.indexOf(v.id);
      return w - f;
    }), d.recomendedWallets = a;
  } else {
    const { chains: s2, isAuth: o } = c2.state, a = s2 == null ? void 0 : s2.join(","), u = i.isArray(t2), v = { page: 1, sdks: o ? "auth_v1" : void 0, entries: i.RECOMMENDED_WALLET_AMOUNT, chains: a, version: 2, excludedIds: u ? t2.join(",") : void 0 }, { listings: w } = M ? await m.getMobileListings(v) : await m.getDesktopListings(v);
    d.recomendedWallets = Object.values(w);
  }
  return d.recomendedWallets;
}, async getWallets(e2) {
  const t2 = B({}, e2), { explorerRecommendedWalletIds: s2, explorerExcludedWalletIds: o } = C.state, { recomendedWallets: a } = d;
  if (o === "ALL")
    return d.wallets;
  a.length ? t2.excludedIds = a.map((k) => k.id).join(",") : i.isArray(s2) && (t2.excludedIds = s2.join(",")), i.isArray(o) && (t2.excludedIds = [t2.excludedIds, o].filter(Boolean).join(",")), c2.state.isAuth && (t2.sdks = "auth_v1");
  const { page: u, search: v } = e2, { listings: w, total: f } = M ? await m.getMobileListings(t2) : await m.getDesktopListings(t2), E = Object.values(w), A = v ? "search" : "wallets";
  return d[A] = { listings: [...d[A].listings, ...E], total: f, page: u ?? 1 }, { listings: E, total: f };
}, getWalletImageUrl(e2) {
  return m.getWalletImageUrl(e2);
}, getAssetImageUrl(e2) {
  return m.getAssetImageUrl(e2);
}, resetSearch() {
  d.search = { listings: [], total: 0, page: 1 };
} };
var I = proxy({ open: false });
var Q = { state: I, subscribe(e2) {
  return subscribe(I, () => e2(I));
}, async open(e2) {
  return new Promise((t2) => {
    const { isUiLoaded: s2, isDataLoaded: o } = c2.state;
    if (c2.setWalletConnectUri(e2 == null ? void 0 : e2.uri), c2.setChains(e2 == null ? void 0 : e2.chains), N.reset("ConnectWallet"), s2 && o)
      I.open = true, t2();
    else {
      const a = setInterval(() => {
        const u = c2.state;
        u.isUiLoaded && u.isDataLoaded && (clearInterval(a), I.open = true, t2());
      }, 200);
    }
  });
}, close() {
  I.open = false;
} };
var H = Object.defineProperty;
var D = Object.getOwnPropertySymbols;
var V = Object.prototype.hasOwnProperty;
var K = Object.prototype.propertyIsEnumerable;
var j = (e2, t2, s2) => t2 in e2 ? H(e2, t2, { enumerable: true, configurable: true, writable: true, value: s2 }) : e2[t2] = s2;
var z = (e2, t2) => {
  for (var s2 in t2 || (t2 = {}))
    V.call(t2, s2) && j(e2, s2, t2[s2]);
  if (D)
    for (var s2 of D(t2))
      K.call(t2, s2) && j(e2, s2, t2[s2]);
  return e2;
};
function J() {
  return typeof matchMedia < "u" && matchMedia("(prefers-color-scheme: dark)").matches;
}
var b = proxy({ themeMode: J() ? "dark" : "light" });
var X = { state: b, subscribe(e2) {
  return subscribe(b, () => e2(b));
}, setThemeConfig(e2) {
  const { themeMode: t2, themeVariables: s2 } = e2;
  t2 && (b.themeMode = t2), s2 && (b.themeVariables = z({}, s2));
} };
var g = proxy({ open: false, message: "", variant: "success" });
var Y = { state: g, subscribe(e2) {
  return subscribe(g, () => e2(g));
}, openToast(e2, t2) {
  g.open = true, g.message = e2, g.variant = t2;
}, closeToast() {
  g.open = false;
} };
typeof window < "u" && (window.Buffer || (window.Buffer = import_buffer.Buffer), window.global || (window.global = window), window.process || (window.process = { env: {} }), window.global || (window.global = window));

export {
  N,
  i,
  $,
  c2 as c,
  C,
  G,
  Q,
  X,
  Y
};
//# sourceMappingURL=chunk-O7ELFTAT.js.map
